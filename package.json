{
  "name": "mod-5-review-backend-fundamentals",
  "version": "1.0.0",
  "description": "Checkpoint 5.1 Review: Backend Fundamentals — HTTP, Express, MVC, REST",
  "private": true,
  "scripts": {
    "server": "node server/server.js",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"npm run server\" \"npm run frontend\"",
    "start": "npm run build && node server/server.js"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
